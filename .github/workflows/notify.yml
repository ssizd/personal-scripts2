name: Discord Notifier

on:
  schedule:
    # Run every 5 minutes, but skip KST 00:00-08:00 (UTC 15:00-23:00)
    # KST 08:00-23:59 = UTC 23:00-14:59
    - cron: '*/5 0-14 * * *'   # UTC 00:00-14:59 (KST 09:00-23:59)
    - cron: '*/5 23 * * *'     # UTC 23:00-23:59 (KST 08:00-08:59)
  workflow_dispatch:  # Manual execution

jobs:
  notify:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        pip install -r requirements.txt

    - name: Run Discord notifier
      env:
        DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        PIXIV_USER_ID: ${{ secrets.PIXIV_USER_ID }}
        PIXIV_REFRESH_TOKEN: ${{ secrets.PIXIV_REFRESH_TOKEN }}
        TWITTER_API_KEY: ${{ secrets.TWITTER_API_KEY }}
        TWITTER_API_SECRET: ${{ secrets.TWITTER_API_SECRET }}
        TWITTER_ACCESS_TOKEN: ${{ secrets.TWITTER_ACCESS_TOKEN }}
        TWITTER_ACCESS_TOKEN_SECRET: ${{ secrets.TWITTER_ACCESS_TOKEN_SECRET }}
        TWITTER_BEARER_TOKEN: ${{ secrets.TWITTER_BEARER_TOKEN }}
        TWITTER_USERNAMES: ${{ secrets.TWITTER_USERNAMES }}
      run: python -u discord_notifier.py

    - name: Commit notified IDs
      run: |
        git config --local user.email "github-actions[bot]@users.noreply.github.com"
        git config --local user.name "github-actions[bot]"
        git add notified_ids.json
        git diff --quiet && git diff --staged --quiet || git commit -m "Update notified IDs [skip ci]"

    - name: Pull latest changes
      run: git pull --rebase origin main

    - name: Push changes
      uses: ad-m/github-push-action@master
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
